export interface Lobby {
    _id: string,
	title: string,
    sport: string,
    dateTime: string,
    dueDateTime: string,
    description: string,
    location: string,
    maxPlayers: number
    author_username: string
}